# Проект: Hub-Solo AmoCRM — Крона Парк

## Контекст бизнеса

- **Продукт**: земельные участки «Крона Парк», эконом-класс
- **Модель**: соло-оператор — один человек ведёт все продажи, без менеджеров и РОПа
- **Стек владельца** (Python): avito-telegram-bot (мониторинг Avito), 24filldoc (генерация ДКП DOCX/PDF)
- **Источники лидов**: Avito, лендинг krona-park.ru, телефония, Telegram
- **Мессенджеры**: Telegram + Max (ВКонтакте). WhatsApp и Viber НЕ используются
- **Obsidian vault владельца**: `D:\00-Obsidian\MyBrainLab` — там материалы по боту
  «Нексус», сессиям и Крона-Парк (см. «Следующие шаги»)

## Что сделано (ветка claude/hub-amocrm-workflow-diagram-bnzczs)

| Файл | Содержание |
|---|---|
| `hub-amocrm-diagram.html` | Блок-схема хаба v1 (устаревшая, под команду — оставлена для сравнения) |
| `hub-audit.md` | Аудит v1: 11 критических замечаний + выводы анализа воронки |
| `hub-amocrm-diagram-v2.html` | **Актуальная схема Hub-Solo v2.2** |
| `index.html`, `assets/` | Исходный сайт «как добраться» (не трогать) |

## Ключевые решения (согласованы с владельцем)

1. **Hub-Solo** = свой Python-сервис: FastAPI + SQLite + APScheduler. Без Redis/HMAC.
2. **Переход Blue/Green**: новый эндпоинт `/webhook/v2`, старые потоки не ломаются,
   avito-telegram-bot и 24filldoc не трогаются до Фазы 2. Откат = смена URL.
3. **Авто-первое-касание** — главный рычаг соло: мгновенный квалифицирующий ответ
   через канал входа (Avito-чат / Telegram / Max), пока оператор занят.
   ⚠️ Тексты черновые — требуют уточнения по материалам Нексуса из Obsidian.
4. **Воронка** (специфика земли, эконом-класс):
   `Новый лид → Квалифицирован → Показ назначен → ★Показ состоялся (выбрал участок)
   → Бронь·Принимает решение (резерв 2 дня) → Оформление документов (запись в МФЦ
   сразу + ДКП параллельно) → Сделка в МФЦ (договор+регистрация+расчёт за 1 визит)
   → Успех | Отказ`
   - Бронь ≠ ПДКП, это просто резерв на 2 дня
   - Сгоревшая бронь → возврат в «Показ состоялся» с задачей, НЕ в отказ
   - Бронь предлагать прямо на участке (QR-табличка = мгновенная бронь)
   - Custom field «Этап отказа» для сегментации реактивации
5. **Telegram и Max — входящие каналы** (Слой 0 в схеме): боты @KronaParkBot + Max-бот,
   deep-links с меткой источника (?start=avito|lp|qr|channel), точки входа: кнопки на
   лендинге, ссылки в Avito-объявлениях, QR на участке, TG-канал. Лид создаётся по
   chat_id без телефона; дедуп по chat_id → merge при появлении телефона.
6. **Дедупликация бессрочная** по телефону (цикл сделки 2–8 недель).
7. **Реактивация**: +30 дней + сезонная рассылка каждый март (вся база отказов за 2 года).
8. **Метрики (4 рычага)**: время первого касания, конверсия Показ→Бронь (цель >25%,
   % сгоревших броней — порог ~40%), CPL по каналу, % дошедших до показа.
   Еженедельный TG-отчёт по понедельникам 9:00. Без дашбордов.
9. **Дорожная карта**: Фаза 1 MVP (webhook→АМО→TG-уведомление+авто-касание, только
   лендинг) → Фаза 2 (боты TG/Max, дедуп, напоминания, показ) → Фаза 3 (24filldoc,
   отчёты, реактивация).

## Следующие шаги (для сессии на ПК)

1. **Прочитать Obsidian vault** `D:\00-Obsidian\MyBrainLab`:
   - найти всё по боту «Нексус» — что это, какая роль; решить, заменяет ли он
     @KronaParkBot в схеме или дополняет
   - материалы по сессиям
   - все заметки по Крона-Парк (цены, рассрочка, акции, свободные участки)
2. **Уточнить тексты авто-первого-касания** в `hub-amocrm-diagram-v2.html`
   (блок «Авто-первое-касание — тексты по каналу и времени суток») по материалам
   Нексуса и фактам о продукте
3. Изучить сайт krona-park.ru (с ПК доступен) — сверить факты для текстов бота
4. Обновить схему v2 и аудит, закоммитить в ту же ветку

## Правила

- Ветка: `claude/hub-amocrm-workflow-diagram-bnzczs`. Пушить только в неё.
- PR не создавать без явной просьбы владельца.
- Схемы — самодостаточные HTML без внешних зависимостей, тёмная тема,
  стиль см. в hub-amocrm-diagram-v2.html.
- Язык общения и документов — русский.
